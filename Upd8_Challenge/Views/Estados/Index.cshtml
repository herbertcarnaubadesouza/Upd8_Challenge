@model IEnumerable<Upd8_Challenge.Models.ESTADO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EstadoName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EstadoName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstadoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstadoId })
        </td>
    </tr>
}

</table>
